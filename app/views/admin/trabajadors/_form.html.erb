
<%= form_for([:admin, @trabajador ], :html => { :class => "form-horizontal" }) do |f| %>
  <% if @trabajador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trabajador.errors.count, "error") %> prohibited this trabajador from being saved:</h2>

      <ul>
        <% @trabajador.errors.full_messages.each do |msg| %><li><%= msg %></li><% end %>
      </ul>
    </div>
  <% end %>
      <div class="control-group">
      <%= f.label :rut, t(:rut, :default => "rut").capitalize, :class => "control-label" %>
      <div class="controls">
        <%= f.number_field :rut %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :nombre, t(:nombre, :default => "nombre").capitalize, :class => "control-label" %>
      <div class="controls">
        <%= f.text_field :nombre %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :materno, t(:materno, :default => "materno").capitalize, :class => "control-label" %>
      <div class="controls">
        <%= f.text_field :materno %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :paterno, t(:paterno, :default => "paterno").capitalize, :class => "control-label" %>
      <div class="controls">
        <%= f.text_field :paterno %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :perfil, t(:perfil, :default => "perfil").capitalize, :class => "control-label" %>
      <div class="controls">
        <%= f.text_field :perfil %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :profesion, t(:profesion, :default => "profesion").capitalize, :class => "control-label" %>
      <div class="controls">
        <%= f.text_field :profesion %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :email, t(:email, :default => "email").capitalize, :class => "control-label" %>
      <div class="controls">
        <%= f.text_field :email %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :sueldo, t(:sueldo, :default => "sueldo").capitalize, :class => "control-label" %>
      <div class="controls">
        <%= f.number_field :sueldo %>
      </div>
    </div>
<!-- Beautiful_scaffold - AddField - Do not remove -->
  <div class="form-actions">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>


