<h2><%= t(:editing, :default => 'Editing') %> <%= t(:categories, :default => "categories") %></h2>

<%= render 'form' %>

<%= link_to t(:back, :default => "Back"), admin_categories_path, :class => "btn" %>